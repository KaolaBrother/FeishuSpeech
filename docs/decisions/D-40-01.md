# D-40-01: One durable review preview and application-bound delivery after a non-secure AX miss

- Status: accepted and implemented; v3 UI correction locally validated; installed Release UAT v3 failed/open
- Date: 2026-08-23
- Issue: #40
- Supersedes in part: D-38-01's strict-AX-only review startup and delivery admission, its selectable
  compatibility/direct-output branch, and its readiness/delivery recovery-copy behavior
- Retains: D-38-01's exact-cursor preference, clipboard transaction, and independent
  capture/recognition axes; D-39-01's Return/Enter keyboard policy

This record is the current Issue #40 authority. Earlier D-40 candidate wording that described
automatic recovery copy or a selectable compatibility route is historical context and is superseded
by the v2 one-route contract. The installed-UAT v3 correction below is a presentation/readiness
amendment; it does not reopen the superseded output routes or change the protected asynchronous roots.

## v3 installed-UAT correction (current UI authority)

The installed Release candidate exposed three presentation defects: the transcript/editor was too
small, read-only content could occupy the titlebar/traffic-light region, and pending UI exposed a
`重试编辑` action. The correction is deliberately bounded:

- the shared transcript preview/editor font is 18pt;
- the retained review panel remains 520x320 initially, with 420x240 minimum and 760x600 maximum;
- `.fullSizeContentView` is not used, keeping read-only content below the titlebar controls;
- an accessory-app activation request is advisory only; actual application-active, panel-key,
  editor-materialized/attached, and editor-first-responder predicates remain fail-closed;
- pending UI has no visible `重试编辑` control and cannot Send/confirm; only `.editable` exposes
  Send and unmodified Return/Enter or Command+Return. Shift+Return/Enter remains newline insertion.

These changes do not alter the one preview route, independent capture/recording and
recognition/provider roots, action-2 plus recorder-barrier draft freeze, exact-target preference,
fixed-PID fallback, or zero-copy/direct-output/automatic-retry/retarget guarantees. The installed
UAT v3 receipt remains failed/open; local validation is not an installed-UAT pass or issue-closure
claim.

## Context

D-38-01 correctly required review-first to bind the original destination before audio or provider
work. Its strict capture contract, however, treated an ordinary non-secure editable target that
could not expose a complete AX cursor, selection, or settable attribute as a startup failure. That
made the review surface show `无法确认输入位置` even when the original application was identifiable
and safe to reactivate.

Issue #40 adds a typed application-bound review authority for that one case and repairs the
candidate's transient-editor failure. A complete original application identity is captured before
the panel, recorder, or provider starts. A safe exact AX cursor remains preferred. When only the
strict cursor contract misses for an ordinary non-secure target, confirmation may reactivate that
exact application and use its current focus. This is an application-bound fallback, not an
ambient-focus or cross-application fallback.

Every accepted interaction, regardless of the persisted `reviewBeforeInsert` or `autoInsert` value,
uses one generation-scoped preview: `streaming -> sealing -> editablePending -> editable`.
Recognition/provider and capture/recording remain independent asynchronous roots. The only external
output gate is an explicit Send click or bare Return/Enter (including Command+Return); Shift+Return
or Shift+Enter inserts one LF. The frozen draft remains coordinator-owned until explicit delivery
succeeds or the user explicitly discards it.

## Decision

### Typed capture outcomes and identity-first ordering

Review destination capture has two successful bindings:

- `ReviewDestinationBinding.exactCursor(CursorDestinationToken)` — the D-38 route, including the
  original `AXUIElement` and selected range;
- `ReviewDestinationBinding.applicationCurrentFocus` — the original complete application identity,
  with no AX element, selection, second PID, app name, or transcript.

`ReviewDestinationToken` carries the positive interaction generation, the complete
`ReviewApplicationIdentity` (PID, bundle identifier, executable URL, and finite launch date), the
binding, and a captured `.safe` security state. Exact cursor generation/PID must match the
application identity. The fallback intentionally carries no cursor that a caller could mistake for
exact authority.

`MacAccessibilityClient.captureReviewCursorDestination` returns a typed
`ReviewCursorCaptureResult`:

- `.exact(...)` for a supported safe element with a valid selection and settable focus/range;
- `.nonSecureCursorUnavailable` for a capability miss after trust and live Secure Input checks
  remain clear; and
- `.rejected(.secureInput)` or `.rejected(.accessibilityUnavailable)` for a secure or untrusted
  condition.

`SystemReviewDestinationDelivery.capture` first captures the frontmost complete application
identity, confirms that the running process with that PID has the same identity, then asks AX for
its typed result. It revalidates both running and frontmost identity afterward. Only an exact result
or an ordinary non-secure capability miss can be admitted. Missing identity fields, PID reuse,
identity drift, lost Accessibility trust, global Secure Input, or an affirmatively secure AX role
map to a terminal startup rejection. The fallback never binds an application discovered after the
review panel changes focus.

### Lifecycle and asynchronous boundaries

Both bindings are captured before `showOverlay`, recorder/ingress setup, `captureDrainTask`, the
recognition consumer, provider startup, or the first review render. Once accepted, both use one
retained panel: streaming -> sealing -> editablePending -> editable, with action-2 and
recorder-barrier ordering and exact untrimmed draft freezing. Fn release only closes capture and
projects sealing; it does not destroy the panel. D-39-01 is unchanged:
unmodified Return/Enter confirms, Shift+Return/Enter inserts LF, Command+Return remains available,
and marked-text Return stays with the input method.

The capture/journal producer and recognition/factory/retry/replay/consumer line remain independent.
Review rendering and delivery introduce no wait, continuation, buffer, or backpressure edge to
either line. Review-first still never writes recognition text while Fn is held or while action 2 is
settling. After action 2 and the recorder barrier, speech-session cleanup may return the hot-key
axis to idle without revoking the live draft authority.

### Confirmation delivery

The exact binding retains D-38 ordering: activate the captured application once, focus only the
captured AX element, restore and reread its original selection, recheck identity/frontmost/security,
then perform one process-targeted Cmd+V transaction. Exact AX restoration remains preferred whenever
it is available.

For `applicationCurrentFocus`, no AX setter or later AX recapture is allowed. After activation, two
consecutive pre-mutation samples must keep Secure Input off, the raw frontmost PID equal to the PID
in the captured identity, and both the running and frontmost complete identities equal to that
identity. `SystemFinalTextOutput.insertReviewAtCurrentFocusOnce` writes the exact untrimmed multiline
draft and posts one Cmd+V pair to that captured PID. It never discovers or substitutes an ambient
PID. Each preflight composite is ordered Secure Input at start -> raw captured PID -> running and
frontmost complete identities -> Secure Input at end; postflight uses the equivalent composite
safety shape.

Activation, security, PID, identity, unsafe-text, key-post, or postflight uncertainty is terminal for
that delivery attempt. There is no automatic retry, retarget, AX recapture, or recovery copy after
a possible post. The same frozen draft returns to `editablePending`/`editable` with fixed,
non-sensitive feedback; a later delivery exists only after a new explicit Send/Return. Readiness
activation, panel-key, editor-materialization, first-responder, cancellation, and timeout failures
likewise retain the same panel and draft. Pending remains non-confirmable without a visible Retry
Editing control; the controller may re-evaluate actual readiness, and Discard remains explicit. Text safety
continues to admit LF and reject NUL, tab, CR, DEL, and C1 controls before mutation. Successful exact
and fallback delivery share the full pasteboard snapshot and `changeCount`-guarded restoration
transaction; a failed or uncertain delivery performs no automatic copy, retry, or retarget.

### Capability limit

The fallback proves the exact original application, not the original control or caret inside that
application. After the review panel is dismissed and the application is reactivated, delivery uses
that application's current focus. It cannot restore an unavailable AX element or selection, and it
cannot cross to whichever application happens to be frontmost. Supporting unavailable intra-app
control/caret authority would require a different platform mechanism and is outside Issue #40.

## Consequences and UAT boundary

An ordinary final-only or editable target that previously missed the strict AX cursor contract can
now open the review surface and complete the same preview, sealing, editing, and confirmation flow.
Both legacy route-setting values converge on this route; settings cannot restore direct or continuous
output. A readiness or delivery failure preserves the exact editable draft in process memory and
does not fall back to clipboard recovery.
The startup message `无法确认输入位置` remains expected for global or affirmatively secure input,
lost Accessibility trust, incomplete application identity, PID reuse, or identity drift. A target
that reaches fallback confirmation still needs owner UAT because `CGEvent.postToPid` has no
application-level acknowledgement that a third-party control consumed Cmd+V.

The installed Release UAT matrix must include at least: one target that previously showed
`无法确认输入位置` because of an ordinary strict-AX miss, one exact-AX target, Secure Input and
secure/password rejection, an application switch during confirmation, a multiline draft, and a
forced post/postflight uncertainty with explicit draft retention and user-directed retry/discard. UAT must verify visible behavior
without claiming that local event submission proves target acceptance.

## Verification

The serialized Issue #40 focused command covering
`ReviewDestinationDeliveryTests`, `ReviewFirstMainViewModelTests`, `FinalTextOutputSecurityTests`,
`ReviewPasteboardLifecycleTests`, and `ReviewFirstApplicationFallbackTests` passed 79/79 tests with
zero failures. The full serialized suite passed 442 tests with one intentional live-TCP skip; Debug,
Release, strict SwiftLint, diff hygiene, and protected async-topology checks also passed. Coverage
includes durable pending authority, same-panel readiness re-evaluation, typed exact-vs-fallback capture,
identity-first ordering and revalidation, secure/incomplete/drifting rejection, fixed-PID multiline
Cmd+V, two consecutive composite security/PID/identity samples with Secure Input rechecks, post
uncertainty, pasteboard restoration, preview/retry independence, and zero-copy failure return. This
focused evidence does not replace installed Release UAT or prove third-party
WindowServer activation and Cmd+V consumption.

The subsequent installed-UAT v3 run remains a failure/open result: it observed the pre-correction
retry-editing control, undersized transcript, and titlebar/traffic-light overlap. The v3 local UI
and readiness correction is therefore documented as implemented and locally validated, but no
installed Release UAT pass or Issue #40 closure is claimed.

No Feishu API request, response, token, transport, dependency, setting schema, entitlement, or
build contract changed; `docs/api.md` is therefore unaffected.
