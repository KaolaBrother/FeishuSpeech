# D-39-01: Review editor Return confirmation policy

- Status: accepted and implemented; keyboard-policy validation complete; Issue #40 v2 retains this
  policy; installed Release UAT pending
- Date: 2026-08-23
- Issue: #39
- Supersedes: D-38-01's editable-review bare-Return editing rule only
- Retains: D-38-01's original-target capture as amended by D-40-01, clipboard lifecycle,
  capture/journal production, recognition/retry/replay consumption, and independent review-axis
  decisions. Issue #40 v2 additionally makes the draft durable across readiness/delivery failure;
  that recovery contract does not change this keyboard policy.

## Context

The historical Issue #38 contract made the review editor multiline and assigned bare Return to text
editing. The shipped review surface now routes Return at the native AppKit editor boundary so
confirmation cannot be mistaken for ordinary text insertion, while preserving multiline editing
and the current review authority boundaries. The review surface is the only route for accepted
interactions; legacy settings cannot restore direct output.

## Decision

- In an editable review, unmodified Return and keypad Enter confirm the current exact draft once.
- Shift+Return and Shift+Enter insert one LF and never confirm.
- Command+Return remains an explicit confirmation shortcut.
- Return while the editor has marked IME text is passed to the input method; the review shortcut does
  not consume that composition event.
- The `发送` button confirms. `取消`, Escape, and closing the editable window discard without target
  or pasteboard mutation. A whitespace-only draft remains editable but cannot confirm.

This decision changes only editable review UI keyboard routing. Captured target authority and
selection semantics remain D-38-01 as amended by D-40-01; confirmation delivery and clipboard
restoration, capture/journal production, recognition/retry/replay consumption, and the independent
review axis are unchanged. The recording overlay remains status-only. D-27-01's direct/continuous
writer is historical and is not an alternate route for an accepted Issue #40 interaction.

## Consequences and verification boundary

The review editor remains multiline and keeps native selection, undo, scrolling, and IME behavior;
only the unmarked Return path is now a confirmation authority. Command+Return remains available for
existing users and keypad Enter follows the same unmodified/Shift/Command policy as the main Return
key.

The issue #39 candidate passes 40/40 focused tests and 423 executed / 1 skipped / 0 failures for the
full suite. Debug and Release builds and strict SwiftLint are green. The focused coverage includes
main and keypad Return/Enter, Shift newline insertion, Command confirmation, marked
IME text pass-through, whitespace rejection, exact untrimmed draft delivery, cancel/discard paths,
and native editor selection, undo, accessibility, and scrolling behavior. Installed Release UAT
with live WindowServer, Accessibility, real credentials, and third-party target acceptance remains
outside this automated evidence. The later Issue #40 v2 gates record 79/79 focused and 442 full
serialized tests (one intentional live-TCP skip), while retaining the same keyboard matrix; those
results do not replace installed Release UAT.
