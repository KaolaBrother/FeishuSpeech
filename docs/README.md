# Documentation Index

- [Architecture](architecture.md)
- [API](api.md)
- [Streaming speech and review-first implementation/design](streaming-speech-design.md) — Issue #40 v4 makes every accepted interaction converge through one same-panel `streaming -> sealing -> editable -> confirming` preview; capture/recording and recognition/provider remain independent asynchronous roots; action 2 plus the recorder barrier freezes a durable draft and exposes Send/qualified Return immediately; focus activation is advisory telemetry only; the preview/editor use 18pt typography in the unchanged 520×320 panel (min/max unchanged); there is no retry-editing control; exact AX remains preferred and an ordinary non-secure strict-AX miss uses the captured application's fixed-PID current-focus fallback; confirmation emits one capped, provenance-checked Unicode pair only after a real UI intent, with no clipboard/Cmd+V path and no automatic retry/retarget; legacy settings are decode-only. Drain expiry retains an exact LF-capable, non-authoritative `ReviewReadOnlyPhase.recovery` surface with no Send/Return/delivery until authoritative action 2 plus recorder barrier. R4 final focused matrix is 265 passed/0 skipped/0 failed, all 105 streaming tests execute, and the full target is 483 passed plus one unrelated live-TCP skip; replacement installation and owner UAT remain pending.
- [Capture/recognition split and direct-connect fallback (confirmed design)](designs/capture-recognition-split-direct-connect.md) — issues #28–#31; owner-confirmed 2026-08-20; capture/recognition/transport ownership remains applicable; output-boundary notes are historical after Issue #40 v2; streaming transport order later superseded by D-32-01, then D-34-01 for hop/DNS
- [Conventions](conventions.md)
- [Decisions](decisions/)
  - [D-18-01: Feishu credential Keychain storage](decisions/D-18-01.md) — issue #18；#35 AppDelegate 不再 load 凭据；#36 恢复 login-keychain 读写
  - [D-25-01: Cursor-bound streaming speech contract](decisions/D-25-01.md) — 历史合同；输出冲突处由 D-27-01 取代，no-preview/直接输出结论再由 D-40-01 v2 的单一路线取代
  - [D-26-01: Journal-indexed held-response output](decisions/D-26-01.md) — 历史 issue #26 合同；拼接与禁止 Backspace 条款已被取代
  - [D-27-01: Opaque snapshot replacement with owned keyboard reconciliation](decisions/D-27-01.md) — 历史兼容输出合同；保留 snapshot/release-drain/安全拓扑事实，固定目标连续输出不再是可选路线
  - [D-28-01: Capture/recognition split and preferNoProxies fallback](decisions/D-28-01.md) — issues #28–#31；streaming 传输顺序由 D-32-01 取代
  - [D-32-01: Keep-alive primary and skip VPN/TUN](decisions/D-32-01.md) — issue #33；owner 2026-08-21 推翻 Q2-B：keep-alive 为 primary，禁止 `.other`，不绑定 `en0`；URLSession hop / fake-ip 范围由 D-34-01 取代
  - [D-34-01: Bound physical DNS and no URLSession hop](decisions/D-34-01.md) — issue #34；bound UDP/53 + `IP_BOUND_IF` TLS；factory/packet/finish 不 hop URLSession；无 IP 字面量
  - [D-38-01: Review-first live preview and explicit original-target delivery](decisions/D-38-01.md) — issue #38 historical base；由 D-40-01 v4 修订为唯一预览/输出路线；保留 exact AX 优先、capture/recognition 独立拓扑；旧 review pasteboard/changeCount 交易已由 v4 删除
  - [D-39-01: Review editor Return confirmation policy](decisions/D-39-01.md) — issue #39；Return/Enter 确认、Shift+Return/Enter 换行、IME marked text 保持不变；D-40-01 v4 只改变交付事务，不改变键盘策略
  - [D-40-01: One durable review preview and application-bound delivery after a non-secure AX miss](decisions/D-40-01.md) — issue #40 current authority（R4）；两种 legacy setting 值均进入同一 preview；action 2 + recorder barrier 直接进入 `.editable`；drain 到期只保留同一面板的 exact LF-capable、非权威 `ReviewReadOnlyPhase.recovery`，没有 Send/qualified Return、编辑或 delivery，不能越过 action-2 authority；focus 只作 advisory telemetry；真实 Send/qualified Return 才能授权一次 16,384 UTF-16 上限、固定 PID、无修饰 Unicode pair；提交 gate 先在短临界区外完成 binding-specific 校验，再按 activation→input 顺序重查 modifiers/epochs 并发送 pair；不读写剪贴板、不发 Cmd+V；submitted-unverified 保留草稿；v3 安装候选失败且已停止，R4 focused 265/0/0、full 483 + 1 unrelated skip，v4 replacement install/UAT pending
- [Changelog](../CHANGELOG.md)
