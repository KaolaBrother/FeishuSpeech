# Make every Fn hold stream recognized text into the current cursor before release

- item: Prove which installed build-4 output path produced no visible text while Fn remained held, using privacy-safe runtime evidence and the current coordinator capability routing.
  status: done
  dispatched: investigator inspects only transcript-free unified-log state/action/capability events and installed process metadata, writing `kaola-workflow/issue-26/held-output-runtime-investigation.md`; code-explorer independently traces live, finalOnly, unbound, and first-partial routing, writing `kaola-workflow/issue-26/output-routing-analysis.md`. Neither launches the app, simulates Fn, reads credentials/transcript text, or triggers permissions.
  result: `held-output-runtime-investigation.md` proves all six installed build-4 generations received a usable partial and selected the AX-unavailable current-focus Unicode path; three selected it before release, including one 5.5 seconds early, yet the owner saw no visible held output. The poster reports success without target acknowledgement and incorrectly creates a single key-down event from HID system state while Fn is physically held. Apple SDK headers say login-session posters should not use HID system state, Unicode may be ignored based on perceived event state, and keyboard sequences require key-down/key-up. `output-routing-analysis.md` separately proves initial/rebound AX `.finalOnly` branches intentionally withhold every partial until release.

- item: Define RED-first acceptance coverage that every safe auto-insert cursor capability emits partial text during the Fn hold and treats release only as capture sealing.
  status: done
  dispatched: tdd-guide owns test artifacts for current-focus Unicode delivery and coordinator routing. It will add deterministic RED cases for a modifier-neutral private event source, explicit key-down/key-up delivery to the bound PID, initial and rebound finalOnly partial emission before release, no release-time duplicate/full paste after provisional output, and unchanged Secure Input/PID/focus/retry protections. RED evidence lands in `/tmp/issue26-held-output-red-*`; production files remain untouched.
  result: RED tests landed in `FinalTextOutputSecurityTests.swift`, `CurrentFocusAppendSessionTests.swift`, and `StreamingMainViewModelTests.swift`. Coordinator failures prove initial/rebound finalOnly never created the append owner, never applied the triggering partial, and release performed the first synthetic insertion. Poster tests fail to compile because production lacks the required PID-bearing protocol, private-state unmodified down/up request model, injectable backend, and paired-post implementation. Evidence is under `/tmp/issue26-held-output-red-*`.

- item: Specify the smallest modifier-neutral, PID-bound Unicode event and finalOnly-to-continuous routing correction that preserves exact focus/security ownership.
  status: done
  dispatched: code-architect writes `kaola-workflow/issue-26/held-output-correction-blueprint.md`, covering event-source/flags/down-up semantics, injectable test boundaries, captured-token validation, append/final fallback ownership, retries, sealing, and no-output safety cases; it edits no source or tests.
  result: `held-output-correction-blueprint.md` specifies a private modifier-independent source, explicit empty flags, identical Unicode key-down/key-up pairs posted to the bound PID, deterministic injection, continuous initial/rebound finalOnly ownership with exact captured-token validation, no alternate fallback after provisional attempt/uncertainty, and release as sealing/finalization rather than first-output trigger.

- item: Repair coordinator/output routing so no safe auto-insert path waits for Fn release before its first visible streaming output.
  status: done
  dispatched: implementer owns only `TextInputSimulator.swift`, `CurrentFocusAppendSession.swift`, `MainViewModel.swift`, and `RecordingState.swift`. It implements the RED poster/session/coordinator contracts and truthful fixed feedback without editing tests, transport, audio, hot-key, permissions, entitlements, or project configuration; focused GREEN evidence lands under `/tmp/issue26-held-output-green-*`.
  result: Production now creates a private modifier-independent Unicode key-down/key-up pair with empty flags and posts both to the bound PID; append sessions pass their immutable PID. Initial and rebound finalOnly safely arm continuous output with captured exact-element/PID/Secure Input validation, apply the triggering partial before release, and prohibit one-shot/clipboard fallthrough after provisional ownership. Fixed feedback no longer claims unacknowledged text was preserved. Focused suites pass 82/82 and owned-file SwiftLint reports zero violations.

- item: Independently review continuous-output correctness and security, including focus changes, Secure Input, opaque partial revisions, retries, and release races.
  status: done
  dispatched: code-reviewer and security-reviewer independently audit the full seven-file production/test diff against the runtime evidence, RED contracts, Apple SDK event-state requirements, and the user's release-only-seals contract. They write `kaola-workflow/issue-26/code-review-held-output.md` and `kaola-workflow/issue-26/security-review-held-output.md`; neither edits source/tests or runs the app.
  result: Final independent code and security receipts both PASS with zero blocking findings. They verify private-state modifier-neutral PID-bound paired events, immediate pre-post Secure Input sampling, exact captured destination ownership, fail-closed uncertainty, one-shot zero-post unsafe recovery revalidation, and release-only sealing. Target acceptance remains an explicit owner-UAT boundary because `CGEventPostToPid` provides no acknowledgement.

- item: Close review findings R1–R4 by making Unicode pair construction atomic, sampling Secure Input immediately before posting, and revalidating captured security before zero-post manual recovery.
  status: done
  dispatched: the existing tdd-guide owns the full review-closure test surface: atomic source/down/up construction, zero posts on construction failure, identical source/payload/flags/PID, ordered trace `construct pair -> final Secure Input sample -> adjacent down/up posts`, production captured validation, and the fallback matrix. Manual recovery is allowed exactly once only for unsafe retained text with zero provisional attempt and a freshly valid captured AX element/PID/security state; it is forbidden after attempt, uncertainty, security, destination loss, or failed revalidation. Evidence lands under `/tmp/issue26-held-output-r1-red-*` and `/tmp/issue26-held-output-r3-red-*` before implementer repair.
  result: R1–R4 are closed test-first. Source/down/up are constructed atomically; a final live Secure Input sample occurs immediately before adjacent down/up posts; any construction failure produces zero posts; and unsafe zero-post manual recovery closes eligibility before freshly validating live Secure Input plus the retained token's security, PID, and exact focused AX element. Focused review-closure suites pass 94/94 and the full lifecycle-free suite passes 263/263.

- item: Dock the corrected contract, run lifecycle-free tests/lint/Debug/Release validation without launching the app, and install the sole incremented Release for owner UAT.
  status: in-flight
  dispatched: doc-updater aligns README, changelog, API/architecture/streaming design, and decision documentation with the verified held-output contract while preserving the owner-UAT boundary. The root agent owns lifecycle-free validation, Release build/signature/metadata checks, sink, and single-copy `/Applications` replacement without launching the app or triggering permissions.

- item: Finalize and sink issue #26 while keeping it open until owner UAT proves real Fn-held streaming output.
  status: in-flight
  dispatched: the root agent runs the consumer validation receipt, run-gap sweep, finalization transaction, merge sink with `comment_keep_open`, closure audit, and exact archive-collision preservation. The issue stays open; no app launch, Fn simulation, credential use, or permission interaction occurs.
