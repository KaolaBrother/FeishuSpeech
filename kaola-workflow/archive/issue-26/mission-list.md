# Fix repeated stream-failure notification after the first successful recognition

- item: Trace the latest successful-recognition-then-failure sequence to the exact action, terminal state, and notification re-entry path without reading credentials or transcript content.
  status: done
  dispatched: investigator performs a privacy-safe read-only analysis of current unified logs and the installed process state, then writes `/Users/ylpromax5/Workspace/feishuspeech/kaola-workflow/issue-26/post-success-live-investigation.md`; it may not launch the app, read credentials/transcripts/raw bodies, make API calls, or trigger permissions.
  result: `post-success-live-investigation.md` proves two accepted HTTP-200 packets were followed by action=0/sequence=2 returning HTTP 200 with business code 10024, then three owner retries each failed action=1/sequence=0 with the same code. Each session published exactly one error, so this is not recursive notification. No action 2 or 3 followed any rejection because the session records `.failed` before coordinator cancellation and `cancel()` returns early for every non-`.none` terminal state.

- item: Verify the authoritative meaning and lifecycle implications of Feishu business code 10024 before choosing whether the repair belongs in packet pacing, stream teardown, or both.
  status: done
  dispatched: knowledge-lookup searches only current official Feishu speech-to-text documentation and returns cited evidence to `/Users/ylpromax5/Workspace/feishuspeech/kaola-workflow/issue-26/business-code-10024.md`; it does not use credentials or call the speech API.
  result: `business-code-10024.md` finds no authoritative definition for code 10024 in current Feishu/Lark docs or official generated SDK. Official boundaries are only 100–200 ms audio per chunk, 20 concurrent stream IDs, action 2 finish/action 3 abort, and the unrelated standard rate-limit code 99991400; packet pacing, leaked lifecycle, sequence/action, tenant, and concurrency causes remain hypotheses rather than facts.

- item: Design a resilient hold lifecycle that aborts failed streams and retries recoverable packet/session failures with bounded backoff while Fn remains held, without publishing an early failure or creating retry storms.
  status: done
  dispatched: code-architect defines the dependency-safe coordinator/session changes, retry authority and cancellation semantics, audio preservation boundaries, error classification, and acceptance tests in `/Users/ylpromax5/Workspace/feishuspeech/kaola-workflow/issue-26/resilient-hold-blueprint.md`; it edits no production or tests and performs no live interaction.
  result: `resilient-hold-blueprint.md` defines exact-once action-3 teardown for failed established streams, fresh-session ordered audio replay across one retained capture, retry-until-release with deterministic 250 ms–4 s capped backoff, release/reset/security race ownership, one terminal feedback, and first-partial opportunistic AX binding. It explicitly treats 10024 as a narrow user-selected retry policy rather than a documented provider meaning.

- item: Compare FeishuSpeech post-success finish/cancel handling with KaolaTerminal's proven stream termination behavior and identify the smallest causal difference.
  status: done
  dispatched: code-explorer reads both repositories' canonical instructions and traces action-2 payload, terminal response handling, state publication, and error-notification behavior, then writes `/Users/ylpromax5/Workspace/feishuspeech/kaola-workflow/issue-26/terminal-reference-comparison.md`; it performs no app launch, live request, credential access, or source edit.
  result: `terminal-reference-comparison.md` proves FeishuSpeech and KaolaTerminal match on action-2 empty audio, sequence, response parsing, and terminal error rules. A visible partial proves only action 1/0 succeeded; a later action-2 failure preserves that text but publishes stream failure. No same-generation duplicate publication path is present in current source, so current safe logs must classify whether the report is one terminal failure per interaction or an installed-runtime repetition.

- item: Replace the current unbound final-only fallback with continuous cursor output while Fn remains held, using replacement rather than duplicate append semantics and retaining security/focus ownership checks.
  status: done
  dispatched: code-explorer maps the live AX and unbound final-only output paths, identifies the smallest safe architecture for current-focus partial replacement without cursor confirmation, and writes `/Users/ylpromax5/Workspace/feishuspeech/kaola-workflow/issue-26/continuous-current-focus-design.md`; it edits nothing and does not control the application or permissions.
  result: `continuous-current-focus-design.md` confirms only verified AX targets currently receive live replacement; unbound/final-only paths intentionally wait for release. The existing current-focus adapter is insert-only and cannot prove replacement ownership. The implementation must therefore add an explicit provisional current-focus replacement session with stable-process/Secure-Input gates and bounded selection replacement, or undertake a much larger IME/input-source architecture; this run will use the scoped session and fail closed on detectable focus/security changes.

- item: Add RED-first regression coverage for failed-stream abort, recoverable retry-until-release without early error publication, and continuous current-focus replacement while Fn is held.
  status: done
  dispatched: tdd-guide first owns only transport/model test artifacts and records baseline RED evidence for typed safe failure metadata plus exact-once action-3 teardown after established action-0/action-2 failures; after that production layer is green, a separate TDD phase will own coordinator retry/replay and first-partial continuous-output tests. Evidence lands under `/tmp/issue26-resilient-*` and results are recorded here.
  result: Three independent TDD surfaces landed RED-first coverage: FeishuStreamingSessionTests proves typed safe codes and exact-once failed-stream action 3; StreamingCoordinatorStateTests/StreamingMainViewModelTests prove retry/backoff/replay/release/lifecycle and continuous-output integration; CurrentFocusAppendSessionTests proves exact UTF-16 suffix-only output, security/PID gates, divergence suppression, and finalization. Baseline failures were missing associated values, abort actions, StreamingRetryPolicy, and append-session types; focused suites are now green at 24/24, 6/6, 34/34, and 16/16.

- item: Repair resilient streaming and continuous-output paths while preserving request serialization, focus ownership, privacy, bounded backoff, and exact-once teardown.
  status: done
  dispatched: implementer first owns `StreamingSpeechModels.swift` and `FeishuStreamingSession.swift` only, making the TDD-owned typed failure and failed-established-stream action-3 tests green without editing tests; after transport proof, coordinator retry/replay and continuous-output production work will be dispatched against its separate RED suite.
  result: Transport now carries only sanitized numeric status/code metadata and aborts failed established streams once. MainViewModel retains one recorder/ingress, journals packets, retries recoverable failures with 250 ms–4 s capped backoff until release, replays in order without historical output churn, and suppresses early failure feedback. CurrentFocusAppendSession supplies clipboard-free, non-destructive first-value/exact-suffix output for unbound targets, while AX live replacement remains authoritative when available.

- item: Review correctness/security, dock the behavior, and validate lifecycle-free tests, strict lint, Debug, and Release builds.
  status: done
  dispatched: code-reviewer and security-reviewer independently audit the full resilient transport/replay/current-focus diff and write receipts under the active issue folder; doc-updater docks only verified behavior and limitations across README/changelog/API/architecture/design/decisions. The main session runs the complete lifecycle-free suite, strict lint, Debug and Release builds without launching the application.
  result: Independent correctness and security receipts both pass with zero blocking findings after R11. The final lifecycle-free direct suite passes 249/249 with zero failures or skips; strict SwiftLint passes 27/27 files with zero violations; Debug build-for-testing and Release 1.0 build 4 both succeed; the Release bundle passes strict deep code-signature verification. No application launch, permission UI, credential access, live API call, or Fn simulation occurred.

- item: Close resilient-hold review findings R1–R8 with RED coverage for release cleanup, narrow retry admission, append outcome/security handling, latest-value retention, and a race-free hold-wide replay budget.
  status: done
  dispatched: the coordinator tdd-guide owns R1–R7 regression changes in its two existing test files; a separate tdd-guide owns only StreamingAudioIngressTests for R8 and defines a delivered-packet retention budget that remains occupancy-based for non-replay ingress. Production fixes will return to implementer only after both RED surfaces land.
  result: The corrected RED suite now exercises real action-2 return/throw failures as well as release during replay/backoff/session creation, strict retry denial, secure/final-only rebind, append outcomes, and last-nonempty retention. Production closes retry admission and cancels the relevant child/attempt exactly once; the ingress charges delivered replay packets against the same 1,920,000-byte capture budget while preserving non-replay occupancy semantics and excluding generated tail silence. Direct integrated XCTest passes 242/242 and strict SwiftLint passes 27/27; independent re-review remains in flight.

- item: Close second-review cancellation/barrier findings with production-typed cancellation, exhaustive sealed output, old-recorder barrier, and complete retry-boundary coverage.
  status: done
  dispatched: the existing coordinator tdd-guide owns deterministic RED tests for established replay cancellation, no-result and append-miss sealed recovery, asynchronous recorder-stop ownership, and typed HTTP/enum boundaries; the implementer owns only MainViewModel/RecordingState production fixes after RED. Evidence is recorded under `/tmp/issue26-r1-*`, `/tmp/issue26-r5-*`, `/tmp/issue26-r9-*`, and `/tmp/issue26-r10-*`.
  result: R1–R11 are resolved. Release/backoff/session-creation/replay cancellation is typed and shared; sealed fallbacks route retained output or one fixed error; HTTP 408/425/429/5xx plus backend 10024 are the only outer retry additions; replay memory stays within one 1,920,000-byte hold budget. R11 immediately revokes generation, output writers, ingress consumer, and transport on abnormal termination while an independently retained recorder barrier blocks successor admission and terminal publication until the old stop completes.

- item: Finalize and sink the repair while keeping issue #26 open for owner UAT, then replace Applications with the sole verified Release without launching it.
  status: in-flight
  dispatched: the main session owns the Kaola finalize/sink transaction and exact Applications replacement. Release 1.0 build 4 is installed and verified as the sole Applications copy without launch; only the merge sink and its closure audit remain.
